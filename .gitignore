# Captured network traffic — never commit
pcap/*.pcap
pcap/*.pcapng

# Generated outputs
output/unique_connections.csv
output/network_diagram.xml

# Override global gitignore — config CSVs belong in the repo
!config/*.csv

# Windows
Thumbs.db
desktop.ini
